/* COMMENT */
// comment
let functionName = function (args) {
	const SCREAMING_SNAKE = '';
	JSON.parse("{}");
	if (args.method !== undefined) {
		let unusedLet = args.property || '';
		let usedLet = args.method(9000);
		throw new Error(`number:\n${SCREAMING_SNAKE} + ${usedLet}`);
	} else {
		return false;
	}
};

const fatArrow = () => {
	label: while (false) { var unreachable; }
	const unusedConst = /^#[0-9A-Z]{6}\b/g;
};

functionName({a:0, b:null, c:'str'});
fatArrow();

const SCREAMING_SNAKE = '';

/* ---------- SPECTRUM TEST 2 ---------- */

scriptName = (a) => { // script name
	let               // keyword
	vari              // identifier
	=                 // operator (asignment)
	'string'          /* string */ ||
	9000              // number
	;                 // terminator


	label:            // label
	while (           // control word
	'flagName'        // string
	=== true          /* operator (comparator) + language constant */ ) { break label; } let _
	= SCREAMING_SNAKE // operator (assignment) + compile time constant


	a.propertyName  /* possibly different from above? */ ||
	/^#[0-9A-Z]{6}\b/g /* not using the red or yellow from this currently */;


	Error    /* dialog identifier*/ (
	a        /* variable identifier (alt: func arg) */ +
	0        /* number */ +
	"string" /* normal string */ +
	"\n"     /* escaped char */ ); _ =


"message"        /* normal message */ +
`${vari}`        /* can't really do xml type stuff here*/+
true             /* language constants I guess */ +
"\n\t\\escape!"  /* escaped chars */ };


unusedLabel:        // unused label
const UNUSED = ''; // unused const
let unused;         // unused let