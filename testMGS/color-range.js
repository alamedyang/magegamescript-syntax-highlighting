/* COMMENT */
// comment
const functionName = function (args) {
	SCREAMING_SNAKE
	JSON.parse("{}")
	if (args.method) {
		let unusedLet = args.property || '';
		const varName = args.method(9000);
		throw new Error(`number:\n${varName}`);
	} else {
		return false;
	}
}

const fatArrow = () => {
	label: while (false) { var unreachable; }
	const unusedConst = /^#[0-9A-Z]{6}\b/g;
}
